<?php

/*
 * api/v3/version.json
 * Return list for specified version
 * @return page
 */

$this->respond('GET', '/[*:version].json', function ($request, $response, $service, $app) {
    
});

/*
 * api/v3/version.md5
 * Return md5 hash for specified version
 * @return page
 */

$this->respond('GET', '/[*:version].md5', function ($request, $response, $service, $app) {
    
});
